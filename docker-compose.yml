services:
  app:
    build: .
    container_name: narap-backend
    ports:
      - "5000:5000"
    env_file: .env
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
